import { RegisterPageModule } from './register-page.module';

describe('RegisterPageModule', () => {
  it('should create', () => {
    // Act
    const module = new RegisterPageModule();

    // Assert
    expect(module).toBeTruthy();
  });
});